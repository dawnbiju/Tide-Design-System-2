name = "tide-figma-webhook-bridge"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put GITHUB_PAT` and `wrangler secret put WEBHOOK_SECRET`)
# Do NOT put real values here — use `wrangler secret put` instead

[vars]
# Non-secret config vars (safe to commit)
GITHUB_REPO = "dawnbiju/Tide-Design-System-2"
